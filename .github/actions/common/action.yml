name: Notify
description: Notify discord and kook app about new release
inputs:
  version:
    description: The new version
  token:
    description: Kook Token
  discord:
    description: Discord webhook
outputs:
  content:
    description: The qq announcement
runs:
  using: 'node16'
  main: index.js
